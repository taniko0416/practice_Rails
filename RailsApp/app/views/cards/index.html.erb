<h1>Cards#index</h1>
<p>Find me in app/views/cards/index.html.erb</p>
<table width="100%">
    <tr>

        <th>ID</th>
        <th>題名</th>
        <th>著者</th>
        <%# 横方向に隣り合うセルの内容が同じであれば、結合の一番左になるセルのtd要素またはth要素にcolspan属性を指定します。
        たとえば、<td colspan=”2″>…</th>という形です。 %>
        <th colspan="2"></th>

    </tr>
    <% @cards.each do |obj| %>
        <tr>
            <td width="35px"><%= obj.id %></td>
            <td width="50%"><a href="/cards/edit<%= obj.id %>">編集</a></td>
            <td><%= obj.author %></td>
            <td width="40px">
                <a href="javascript:window.delDara(
                    <%= (obj.id.to_s + ",''".to_s + obj.title.to_s).html_safe %>');">
                    削除
                </a>
            </td>
        </tr>
    <% end %>
</table>
<p class="link"><a href="/cards/add"></a>&gt;&gt;</p>
